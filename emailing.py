import  imghdr

def send_email(img_path):
    """ Send an email containing an image from the `image_to_send` variable in the main module of this project
    :param 'img_path` Image to be sent as attachment in the email
    """
    return "Email was sent"
